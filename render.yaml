services:
  - type: web
    name: emoji-story-shuffle
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
